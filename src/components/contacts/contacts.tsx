import styles from '@/UI/sass/container.module.scss';

const Contacts = () => {
  return (
    <>
      <section className='min-h-64 bg-red-100'>
        <div className={`${styles.container}`}>Contacts</div>
      </section>
    </>
  );
};

export default Contacts;
